/**
 * Exercise: parseCsv
 *
 * Goal: Parse CSV text (comma-separated, first line is the header row) into an array of row objects keyed by header. Assume no quoted commas. Ignore a trailing empty line.
 *
 * Prerequisites:
 *   - splitting on newlines and commas
 *   - zipping headers with values
 *   - building objects
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/split
 *
 * Time budget: ~20 minutes.
 */

export function parseCsv(text: string): Record<string, string>[] {
  throw new Error('not implemented');
}
