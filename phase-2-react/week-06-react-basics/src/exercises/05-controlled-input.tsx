/**
 * Exercise: Controlled input
 *
 * Goal: A controlled text input that echoes its current value into a separate element with data-testid="echo".
 *
 * Prerequisites:
 *   - controlled inputs (value + onChange)
 *
 * References:
 *   - https://react.dev/learn/reacting-to-input-with-state
 *
 * Rules:
 *   - Echo the current value in an element with data-testid="echo"
 *
 * Time budget: ~10 minutes.
 */

export function ControlledInput(): JSX.Element {
  throw new Error('not implemented');
}
