/**
 * Exercise: Todo list (minimal)
 *
 * Goal: Render a text input and an "Add" button. Typing a value and clicking Add appends it to a list (<ul> of <li>).
 *
 * Prerequisites:
 *   - useState with an array
 *   - immutable updates (spread)
 *   - controlled input
 *
 * References:
 *   - https://react.dev/learn/updating-arrays-in-state
 *
 * Rules:
 *   - The add button must be a <button> whose accessible name contains "Add"
 *   - Render each todo as an <li>
 *
 * Time budget: ~20 minutes.
 */

export function TodoList(): JSX.Element {
  throw new Error('not implemented');
}
