/**
 * Exercise: Toggle button
 *
 * Goal: A single button whose label toggles between "OFF" (initial) and "ON" on each click.
 *
 * Prerequisites:
 *   - boolean state
 *   - toggling with setState(prev => !prev)
 *
 * References:
 *   - https://react.dev/learn/state-a-components-memory
 *
 * Rules:
 *   - Button text must be exactly "ON" or "OFF"
 *
 * Time budget: ~10 minutes.
 */

export function ToggleButton(): JSX.Element {
  throw new Error('not implemented');
}
