/**
 * Exercise: Counter
 *
 * Goal: Render a count starting at 0 and a button that increments it by 1.
 *
 * Prerequisites:
 *   - useState
 *   - event handlers (onClick)
 *
 * References:
 *   - https://react.dev/learn/state-a-components-memory
 *
 * Rules:
 *   - Render the count inside an element with data-testid="count"
 *
 * Time budget: ~12 minutes.
 */

export function Counter(): JSX.Element {
  throw new Error('not implemented');
}
