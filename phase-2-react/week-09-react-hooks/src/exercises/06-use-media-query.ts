/**
 * Exercise: useMediaQuery
 *
 * Goal: A hook useMediaQuery(query): boolean that returns whether the media query currently matches, updating when it changes.
 *
 * Prerequisites:
 *   - window.matchMedia
 *   - subscribing/unsubscribing to a MediaQueryList in an effect
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/API/Window/matchMedia
 *
 * Time budget: ~18 minutes.
 */

export function useMediaQuery(query: string): boolean {
  throw new Error('not implemented');
}
