/**
 * Exercise: useKeyPress
 *
 * Goal: A hook useKeyPress(targetKey): boolean that is true while the given key is held down (keydown) and false after keyup.
 *
 * Prerequisites:
 *   - window keydown/keyup listeners in an effect
 *   - comparing event.key
 *   - cleanup
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key
 *
 * Time budget: ~18 minutes.
 */

export function useKeyPress(targetKey: string): boolean {
  throw new Error('not implemented');
}
