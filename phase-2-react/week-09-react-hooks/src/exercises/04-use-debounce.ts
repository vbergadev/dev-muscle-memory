/**
 * Exercise: useDebounce
 *
 * Goal: A hook useDebounce<T>(value, ms): T that returns the latest value only after it has stayed unchanged for `ms` milliseconds.
 *
 * Prerequisites:
 *   - useState + useEffect
 *   - setTimeout/clearTimeout in an effect cleanup
 *   - debouncing
 *
 * References:
 *   - https://react.dev/reference/react/useEffect
 *
 * Time budget: ~18 minutes.
 */

export function useDebounce<T>(value: T, ms: number): T {
  throw new Error('not implemented');
}
