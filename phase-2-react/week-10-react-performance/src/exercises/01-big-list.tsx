/**
 * Exercise: Big list (measure first)
 *
 * Goal: Render a list of strings, one <li> each. Then open React DevTools Profiler and measure how re-renders behave as the list grows. Correctness first; optimization comes in the next exercises.
 *
 * Prerequisites:
 *   - rendering lists
 *   - React DevTools Profiler (manual)
 *
 * References:
 *   - https://react.dev/reference/react/Profiler
 *
 * Rules:
 *   - Render each item as an <li>
 *
 * Time budget: ~12 minutes.
 */

export function BigList({ items }: { items: string[] }): JSX.Element {
  throw new Error('not implemented');
}
