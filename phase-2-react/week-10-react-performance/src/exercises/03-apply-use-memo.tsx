/**
 * Exercise: useMemo
 *
 * Goal: A component ExpensiveList({ numbers }) that computes the sum of the numbers with useMemo and shows it in an element with data-testid="sum".
 *
 * Prerequisites:
 *   - useMemo and its dependency array
 *   - when memoizing a computation is worth it
 *
 * References:
 *   - https://react.dev/reference/react/useMemo
 *
 * Rules:
 *   - Show the sum in data-testid="sum"
 *
 * Time budget: ~15 minutes.
 */

export function ExpensiveList({ numbers }: { numbers: number[] }): JSX.Element {
  throw new Error('not implemented');
}
