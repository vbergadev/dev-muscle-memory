/**
 * Exercise: Focus an input with useRef
 *
 * Goal: Render a text input and a button. Clicking the button focuses the input.
 *   The input must also be focused automatically once, on mount.
 *
 * Prerequisites:
 *   - useRef to hold a DOM node
 *   - Attaching a ref to a JSX element (ref={...})
 *   - Why focusing is a side effect, not something you do during render
 *   - Reading ref.current safely (it can be null)
 *
 * References:
 *   - https://react.dev/reference/react/useRef
 *   - https://react.dev/learn/manipulating-the-dom-with-refs
 *
 * Rules:
 *   - Do not use document.querySelector — use a ref
 *
 * Time budget: ~15 minutes.
 */

export function FocusInput(): JSX.Element {
  throw new Error('not implemented');
}
