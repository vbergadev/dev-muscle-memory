/**
 * Exercise: Type-safe routing with TanStack Router
 *
 * Goal: Set up a TanStack Router with two routes: "/" (home) and "/posts/$postId" (reads a typed postId param). Export a RouterApp component that renders the RouterProvider. There is no automated test for this one — verify it in the browser with `pnpm dev`.
 *
 * Prerequisites:
 *   - createRouter / createRoute / RouterProvider
 *   - type-safe route params ($postId)
 *   - a root route with an <Outlet />
 *
 * References:
 *   - https://tanstack.com/router/latest/docs/framework/react/overview
 *
 * Rules:
 *   - No library-free requirement here — this exercise is about TanStack Router
 *   - Params must be type-safe (no `any`)
 *
 * Time budget: ~30 minutes.
 */

export function RouterApp(): JSX.Element {
  throw new Error('not implemented');
}
