/**
 * Exercise: Data fetching with TanStack Query
 *
 * Goal: A QueryApp component that uses useQuery to GET "/api/user" (JSON { name: string }) and renders the name. Show "loading" text while the query is pending. Assume it is rendered inside a QueryClientProvider.
 *
 * Prerequisites:
 *   - useQuery: queryKey + queryFn
 *   - pending / error / success states
 *   - QueryClientProvider
 *
 * References:
 *   - https://tanstack.com/query/latest/docs/framework/react/overview
 *
 * Rules:
 *   - Show "loading" while pending
 *   - Render the fetched name
 *
 * Time budget: ~25 minutes.
 */

export function QueryApp(): JSX.Element {
  throw new Error('not implemented');
}
