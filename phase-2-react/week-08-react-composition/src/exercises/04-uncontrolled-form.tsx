/**
 * Exercise: Uncontrolled form with refs
 *
 * Goal: A form with an uncontrolled "username" input (read via a ref on submit, not via state). On submit, display the submitted value in an element with data-testid="result".
 *
 * Prerequisites:
 *   - uncontrolled inputs (defaultValue)
 *   - useRef to read a DOM value on submit
 *   - controlled vs uncontrolled trade-offs
 *
 * References:
 *   - https://react.dev/reference/react-dom/components/input
 *
 * Rules:
 *   - The username input accessible name must match "username"
 *   - Show the result in data-testid="result"
 *
 * Time budget: ~20 minutes.
 */

export function UncontrolledForm(): JSX.Element {
  throw new Error('not implemented');
}
