/**
 * Exercise: Lifting state up
 *
 * Goal: Two text inputs, "a" and "b", that always mirror each other: typing in one updates both. Hold the shared value in the common parent (lifted state), not in each input.
 *
 * Prerequisites:
 *   - lifting state up to a common parent
 *   - passing value + onChange down as props
 *
 * References:
 *   - https://react.dev/learn/sharing-state-between-components
 *
 * Rules:
 *   - Inputs must have accessible names matching "a" and "b"
 *
 * Time budget: ~18 minutes.
 */

export function LiftingState(): JSX.Element {
  throw new Error('not implemented');
}
