/**
 * Exercise: Controlled form with manual validation
 *
 * Goal: A form with a controlled "name" text field and a submit button. On submit: if the name is empty, show an error containing "required"; otherwise show a message containing "submitted".
 *
 * Prerequisites:
 *   - controlled inputs
 *   - form onSubmit + preventDefault
 *   - conditional rendering of validation messages
 *
 * References:
 *   - https://react.dev/learn/reacting-to-input-with-state
 *
 * Rules:
 *   - The name input must have an accessible name matching "name" (e.g. aria-label="name")
 *   - The submit button accessible name must contain "submit"
 *
 * Time budget: ~25 minutes.
 */

export function ControlledForm(): JSX.Element {
  throw new Error('not implemented');
}
