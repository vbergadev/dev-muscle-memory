/**
 * Exercise: removeDuplicates
 *
 * Goal: Return a new array with duplicates removed, preserving first-seen order.
 *
 * Prerequisites:
 *   - tracking seen values
 *   - building a new array
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/includes
 *
 * Rules:
 *   - Do not use `Set`
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function removeDuplicates(arr: number[]): number[] {
  throw new Error('not implemented');
}
