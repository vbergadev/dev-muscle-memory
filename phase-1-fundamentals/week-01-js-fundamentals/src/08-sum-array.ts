/**
 * Exercise: sumArray
 *
 * Goal: Return the sum of all numbers in the array. Sum of an empty array is 0.
 *
 * Prerequisites:
 *   - for...of loops
 *   - accumulator pattern
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for...of
 *
 * Rules:
 *   - Do not use `.reduce()`
 *
 * Time budget: ~5 minutes. If you spend more, review the prerequisites first.
 */

export function sumArray(arr: number[]): number {
  throw new Error('not implemented');
}
