/**
 * Exercise: gcd
 *
 * Goal: Return the greatest common divisor of a and b using Euclid's algorithm.
 *
 * Prerequisites:
 *   - modulo operator
 *   - Euclid's algorithm
 *   - loops or recursion
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Euclidean_algorithm
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function gcd(a: number, b: number): number {
  throw new Error('not implemented');
}
