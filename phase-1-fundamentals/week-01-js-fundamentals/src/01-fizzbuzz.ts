/**
 * Exercise: fizzbuzz
 *
 * Goal: Given a positive integer n, return an array of strings from index 1 to n where multiples of 3 are "Fizz", multiples of 5 are "Buzz", multiples of both are "FizzBuzz", and other numbers are the number as a string.
 *
 * Prerequisites:
 *   - for loops
 *   - modulo operator (%)
 *   - if/else if/else branching
 *   - string vs number conversion
 *
 * References:
 *   - https://javascript.info/while-for
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Remainder
 *
 * Rules:
 *   - Do not use ternary operators (practice explicit if/else)
 *   - Do not use `Array.from` or `Array(n).fill(...)` — build the array with push
 *
 * Time budget: ~10 minutes. If you spend more, review the prerequisites first.
 */

export function fizzbuzz(n: number): string[] {
  throw new Error('not implemented');
}
