/**
 * Exercise: flattenArray
 *
 * Goal: Flatten one level of nesting: turn [1, [2, 3], 4] into [1, 2, 3, 4].
 *
 * Prerequisites:
 *   - Array.isArray
 *   - nested iteration
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/isArray
 *
 * Rules:
 *   - Do not use `.flat()`
 *   - Only one level of depth is required
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function flattenArray(arr: (number | number[])[]): number[] {
  throw new Error('not implemented');
}
