/**
 * Exercise: factorialIterative
 *
 * Goal: Return n! (factorial) using a loop. factorial(0) is 1.
 *
 * Prerequisites:
 *   - for loops
 *   - accumulator pattern
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Factorial
 *
 * Rules:
 *   - Do not use recursion here
 *
 * Time budget: ~6 minutes. If you spend more, review the prerequisites first.
 */

export function factorialIterative(n: number): number {
  throw new Error('not implemented');
}
