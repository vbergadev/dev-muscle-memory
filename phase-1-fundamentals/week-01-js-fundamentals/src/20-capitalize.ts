/**
 * Exercise: capitalize
 *
 * Goal: Uppercase the first character of the string, leaving the rest unchanged.
 *
 * Prerequisites:
 *   - string indexing
 *   - string slicing
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/toUpperCase
 *
 * Time budget: ~5 minutes. If you spend more, review the prerequisites first.
 */

export function capitalize(s: string): string {
  throw new Error('not implemented');
}
