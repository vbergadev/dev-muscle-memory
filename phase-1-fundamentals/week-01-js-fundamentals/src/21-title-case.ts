/**
 * Exercise: titleCase
 *
 * Goal: Capitalize the first letter of every word in the sentence.
 *
 * Prerequisites:
 *   - splitting on spaces
 *   - mapping words
 *   - string joining
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/split
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function titleCase(s: string): string {
  throw new Error('not implemented');
}
