/**
 * Exercise: reverseString
 *
 * Goal: Return the input string reversed, character by character.
 *
 * Prerequisites:
 *   - string indexing
 *   - for loops (counting down)
 *   - string concatenation
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String
 *
 * Rules:
 *   - Do not use `.reverse()`, `.split()`, or spread — build the result with a loop
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function reverseString(s: string): string {
  throw new Error('not implemented');
}
