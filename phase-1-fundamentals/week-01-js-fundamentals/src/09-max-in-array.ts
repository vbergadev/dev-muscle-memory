/**
 * Exercise: maxInArray
 *
 * Goal: Return the largest number in the array. Assume the array is non-empty.
 *
 * Prerequisites:
 *   - for loops
 *   - tracking a running maximum
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/max
 *
 * Rules:
 *   - Do not use `Math.max(...arr)` or `.sort()`
 *
 * Time budget: ~6 minutes. If you spend more, review the prerequisites first.
 */

export function maxInArray(arr: number[]): number {
  throw new Error('not implemented');
}
