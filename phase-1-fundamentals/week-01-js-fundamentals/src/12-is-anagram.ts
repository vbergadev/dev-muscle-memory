/**
 * Exercise: isAnagram
 *
 * Goal: Return true if the two strings are anagrams of each other (same letters, same counts), ignoring case and spaces.
 *
 * Prerequisites:
 *   - character counting with an object/map
 *   - normalization
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/toLowerCase
 *
 * Rules:
 *   - Try to solve in O(n) with a counts map, not by sorting
 *
 * Time budget: ~12 minutes. If you spend more, review the prerequisites first.
 */

type Counts = {
  throw new Error('not implemented');
}
