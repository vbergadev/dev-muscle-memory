/**
 * Exercise: range
 *
 * Goal: Return an array of integers from start to end inclusive. If start > end, return an empty array.
 *
 * Prerequisites:
 *   - for loops
 *   - array push
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array
 *
 * Rules:
 *   - Do not use `Array.from`
 *
 * Time budget: ~6 minutes. If you spend more, review the prerequisites first.
 */

export function range(start: number, end: number): number[] {
  throw new Error('not implemented');
}
