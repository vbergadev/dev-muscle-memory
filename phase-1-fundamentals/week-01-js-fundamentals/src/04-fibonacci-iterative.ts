/**
 * Exercise: fibonacciIterative
 *
 * Goal: Return the nth Fibonacci number (0-indexed: fib(0)=0, fib(1)=1) using iteration.
 *
 * Prerequisites:
 *   - for loops
 *   - tracking two variables across iterations
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Fibonacci_number
 *
 * Rules:
 *   - Do not use recursion here (that is exercise 05)
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function fibonacciIterative(n: number): number | undefined {
  throw new Error('not implemented');
}
