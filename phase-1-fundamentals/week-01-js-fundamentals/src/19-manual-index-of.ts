/**
 * Exercise: manualIndexOf
 *
 * Goal: Reimplement Array.prototype.indexOf: return the first index of target, or -1 if absent.
 *
 * Prerequisites:
 *   - linear search
 *   - returning early
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/indexOf
 *
 * Rules:
 *   - Do not call the built-in `.indexOf()`
 *
 * Time budget: ~6 minutes. If you spend more, review the prerequisites first.
 */

export function manualIndexOf<T>(arr: T[], target: T): number {
  throw new Error('not implemented');
}
