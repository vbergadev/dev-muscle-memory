/**
 * Exercise: fibonacciRecursive
 *
 * Goal: Return the nth Fibonacci number using recursion with a clear base case.
 *
 * Prerequisites:
 *   - recursion and base cases
 *   - the call stack
 *
 * References:
 *   - https://javascript.info/recursion
 *
 * Rules:
 *   - Must be recursive (no loops)
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function fibonacciRecursive(n: number): number {
  throw new Error('not implemented');
}
