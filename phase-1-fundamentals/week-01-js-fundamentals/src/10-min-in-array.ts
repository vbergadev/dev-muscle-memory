/**
 * Exercise: minInArray
 *
 * Goal: Return the smallest number in the array. Assume the array is non-empty.
 *
 * Prerequisites:
 *   - for loops
 *   - tracking a running minimum
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/min
 *
 * Rules:
 *   - Do not use `Math.min(...arr)` or `.sort()`
 *
 * Time budget: ~6 minutes. If you spend more, review the prerequisites first.
 */

export function minInArray(arr: number[]): number {
  throw new Error('not implemented');
}
