/**
 * Exercise: mostFrequent
 *
 * Goal: Return the element that appears most often. Return undefined for an empty array. On ties, return the first to reach the max count.
 *
 * Prerequisites:
 *   - frequency map
 *   - tracking a running best
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map
 *
 * Time budget: ~12 minutes. If you spend more, review the prerequisites first.
 */

export function mostFrequent<T>(arr: T[]): T | undefined {
  throw new Error('not implemented');
}
