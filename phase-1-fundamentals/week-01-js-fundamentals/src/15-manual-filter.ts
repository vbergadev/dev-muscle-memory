/**
 * Exercise: manualFilter
 *
 * Goal: Reimplement Array.prototype.filter: return a new array with only the elements for which predicate returns true.
 *
 * Prerequisites:
 *   - higher-order functions
 *   - callbacks
 *   - generics
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/filter
 *
 * Rules:
 *   - Do not call the built-in `.filter()`
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function manualFilter<T>(arr: T[], predicate: (x: T) => boolean): T[] {
  throw new Error('not implemented');
}
