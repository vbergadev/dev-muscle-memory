/**
 * Exercise: countOccurrences
 *
 * Goal: Count how many times target appears in the array.
 *
 * Prerequisites:
 *   - linear scan
 *   - counter accumulator
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array
 *
 * Rules:
 *   - Do not use `.filter().length`
 *
 * Time budget: ~6 minutes. If you spend more, review the prerequisites first.
 */

export function countOccurrences<T>(arr: T[], target: T): number {
  throw new Error('not implemented');
}
