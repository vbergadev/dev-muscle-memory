/**
 * Exercise: countVowels
 *
 * Goal: Count how many vowels (a, e, i, o, u, case-insensitive) appear in the string.
 *
 * Prerequisites:
 *   - iterating over string characters
 *   - membership checks
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String
 *
 * Rules:
 *   - Do not use regex
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function countVowels(s: string): number {
  throw new Error('not implemented');
}
