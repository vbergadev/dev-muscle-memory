/**
 * Exercise: manualIncludes
 *
 * Goal: Reimplement Array.prototype.includes: return true if target is present.
 *
 * Prerequisites:
 *   - strict equality
 *   - linear search
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/includes
 *
 * Rules:
 *   - Do not call the built-in `.includes()` or `.indexOf()`
 *
 * Time budget: ~5 minutes. If you spend more, review the prerequisites first.
 */

export function manualIncludes<T>(arr: T[], target: T): boolean {
  throw new Error('not implemented');
}
