/**
 * Exercise: isPalindrome
 *
 * Goal: Return true if the string reads the same forwards and backwards, ignoring case and non-alphanumeric characters.
 *
 * Prerequisites:
 *   - two-pointer technique
 *   - string comparison
 *   - character normalization
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/toLowerCase
 *
 * Rules:
 *   - Prefer a two-pointer loop over reversing the whole string
 *
 * Time budget: ~12 minutes. If you spend more, review the prerequisites first.
 */

export function isPalindrome(s: string): boolean {
  throw new Error('not implemented');
}
