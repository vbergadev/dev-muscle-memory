/**
 * Exercise: manualMap
 *
 * Goal: Reimplement Array.prototype.map: return a new array with fn applied to each element.
 *
 * Prerequisites:
 *   - higher-order functions
 *   - generics
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/map
 *
 * Rules:
 *   - Do not call the built-in `.map()`
 *
 * Time budget: ~6 minutes. If you spend more, review the prerequisites first.
 */

export function manualMap<T, U>(arr: T[], fn: (x: T) => U): U[] {
  throw new Error('not implemented');
}
