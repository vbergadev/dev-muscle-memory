/**
 * Exercise: lcm
 *
 * Goal: Return the least common multiple of a and b. Hint: lcm(a,b) = a*b / gcd(a,b).
 *
 * Prerequisites:
 *   - gcd (previous exercise)
 *   - integer arithmetic
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Least_common_multiple
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */
function gcd(a: number, b: number): number {
  throw new Error('not implemented');
}
