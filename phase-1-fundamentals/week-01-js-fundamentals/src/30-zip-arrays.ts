/**
 * Exercise: zipArrays
 *
 * Goal: Zip two arrays into an array of pairs. Stop at the length of the shorter array.
 *
 * Prerequisites:
 *   - parallel iteration
 *   - Math.min
 *   - tuples in TS
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/min
 *
 * Time budget: ~8 minutes. If you spend more, review the prerequisites first.
 */

export function zipArrays<T, U>(a: T[], b: U[]): [T, U][] {
  throw new Error('not implemented');
}
