/**
 * Exercise: makeCounter
 *
 * Goal: Return a function that, each time it is called, increments and returns a private counter starting from `start` (default 0). The first call returns start.
 *
 * Prerequisites:
 *   - closures with private mutable state
 *
 * References:
 *   - https://javascript.info/closure
 *
 * Time budget: ~8 minutes.
 */

export function makeCounter(start?: number): () => number {
  throw new Error('not implemented');
}
