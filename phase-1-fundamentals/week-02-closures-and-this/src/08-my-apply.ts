/**
 * Exercise: myApply
 *
 * Goal: Reimplement Function.prototype.apply: invoke fn with `thisArg` as its `this` and an array (or undefined) of args.
 *
 * Prerequisites:
 *   - same as myCall, but args arrive as an array
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/apply
 *
 * Rules:
 *   - Do not use the built-in `.apply()`, `.call()`, or `.bind()`
 *
 * Time budget: ~12 minutes.
 */

export function myApply(fn: (...args: any[]) => any, thisArg: any, args?: any[]): any {
  throw new Error('not implemented');
}
