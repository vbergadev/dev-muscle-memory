/**
 * Exercise: deepEqual
 *
 * Goal: Return true if a and b are deeply equal (same structure and primitive values) across nested plain objects and arrays.
 *
 * Prerequisites:
 *   - recursion
 *   - comparing keys of both objects
 *   - array length + element comparison
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Equality
 *
 * Time budget: ~20 minutes.
 */

export function deepEqual(a: unknown, b: unknown): boolean {
  throw new Error('not implemented');
}
