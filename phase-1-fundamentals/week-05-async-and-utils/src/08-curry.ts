/**
 * Exercise: curry
 *
 * Goal: Curry a function of fixed arity: allow it to be called with arguments one (or a few) at a time until enough have been collected, then invoke the original.
 *
 * Prerequisites:
 *   - `fn.length` (arity)
 *   - partial application via closures
 *   - recursion or accumulation
 *
 * References:
 *   - https://javascript.info/currying-partials
 *
 * Time budget: ~20 minutes.
 */

export function curry(fn: (...args: any[]) => any): (...args: any[]) => any {
  throw new Error('not implemented');
}
