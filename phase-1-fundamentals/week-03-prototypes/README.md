# Week 3 — Prototypes, classes, inheritance

## Goal of the week
Master the prototype chain, simulate `new`, prototypal inheritance, and converting to ES6 classes.

## Concepts you should own by the end
- Prototype chain and `__proto__` vs `prototype`
- How `new` works under the hood
- Manual prototypal inheritance
- ES6 classes and inheritance

## Required reading
- https://javascript.info/prototypes (full ch. 8)

## Parallel track (DSA)
Chapter 4 (queues and deques). Implement in `dsa/ch-04-queues-deques/`.

## Exercises this week
1. `01-simulate-new.ts`
2. `02-prototypal-inheritance.ts`
3. `03-to-es6-classes.ts`
4. `04-event-emitter.ts`
5. `05-plugin-system.ts`

## How to run
```bash
pnpm install          # (at the root, once)
pnpm test             # run this week's tests (failing on purpose)
pnpm test:watch       # watch mode while you implement
```

## Deliverable by end of week
A working mini plugin framework, with tests.

## Checkpoint
If you can't draw an object's prototype chain on paper, review.
