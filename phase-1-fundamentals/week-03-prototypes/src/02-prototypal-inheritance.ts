/**
 * Exercise: inherit
 *
 * Goal: Set up prototypal inheritance so instances of Child inherit from Parent: Child.prototype should delegate to Parent.prototype, and Child.prototype.constructor should point back to Child.
 *
 * Prerequisites:
 *   - `Object.create`
 *   - prototype vs constructor
 *   - delegation up the chain
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/create
 *
 * Time budget: ~18 minutes.
 */

export function inherit(Child: Function, Parent: Function): void {
  throw new Error('not implemented');
}
