/**
 * Exercise: permutations
 *
 * Goal: Return all permutations of the characters of the input string (assume distinct characters). Order of the result does not matter.
 *
 * Prerequisites:
 *   - recursion with a shrinking remainder
 *   - building combinations
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Permutation
 *
 * Time budget: ~25 minutes.
 */

export function permutations(input: string): string[] {
  throw new Error('not implemented');
}
