/**
 * Exercise: fibonacci (memoized, O(n))
 *
 * Goal: Return the nth Fibonacci number in O(n) time using memoization (or bottom-up), so large n is instant — unlike the O(2^n) naive recursion. fib(0)=0, fib(1)=1.
 *
 * Prerequisites:
 *   - memoization / bottom-up DP
 *   - Big-O: why naive recursion is exponential
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Fibonacci_sequence
 *
 * Time budget: ~18 minutes.
 */

export function fibonacci(n: number): number {
  throw new Error('not implemented');
}
