/**
 * Exercise: bubbleSort & quickSort
 *
 * Goal: Implement bubbleSort(arr) and quickSort(arr), each returning a new ascending-sorted array (do not mutate the input).
 *
 * Prerequisites:
 *   - bubble sort (adjacent swaps)
 *   - quick sort (pivot + partition)
 *   - not mutating the input
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Sorting_algorithm
 *
 * Time budget: ~30 minutes.
 */

export function bubbleSort(_arr: number[]): number[] {
  throw new Error('not implemented');
}

export function quickSort(_arr: number[]): number[] {
  throw new Error('not implemented');
}
