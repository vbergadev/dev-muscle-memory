/**
 * Exercise: binarySearch
 *
 * Goal: Return the index of target in an ascending-sorted array, or -1 if absent. Use binary search (O(log n)).
 *
 * Prerequisites:
 *   - low/high pointers and mid
 *   - halving the search space
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Binary_search_algorithm
 *
 * Time budget: ~18 minutes.
 */

export function binarySearch(sorted: number[], target: number): number {
  throw new Error('not implemented');
}
