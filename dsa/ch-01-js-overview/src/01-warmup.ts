/**
 * Exercise: warm-up (sanity check for the workspace setup)
 *
 * Goal: Return the sum of two numbers. Trivial on purpose — this only exists so
 *   you can confirm this DSA workspace compiles and runs its tests.
 *
 * Prerequisites:
 *   - functions and return values
 *
 * References:
 *   - https://javascript.info/function-basics
 *
 * Time budget: ~2 minutes.
 */

export function sum(a: number, b: number): number {
  throw new Error('not implemented');
}
