/**
 * Exercise: Binary Search Tree
 *
 * Goal: A BST of numbers: insert, has (search), min, max, and inOrder() returning values in sorted order.
 *
 * Prerequisites:
 *   - BST invariant (left < node < right)
 *   - recursive insert/search
 *   - in-order traversal
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Binary_search_tree
 *
 * Time budget: ~35 minutes.
 */

export class BinarySearchTree {
  insert(_value: number): void {
    throw new Error('not implemented');
  }
  has(_value: number): boolean {
    throw new Error('not implemented');
  }
  min(): number | undefined {
    throw new Error('not implemented');
  }
  max(): number | undefined {
    throw new Error('not implemented');
  }
  inOrder(): number[] {
    throw new Error('not implemented');
  }
}
