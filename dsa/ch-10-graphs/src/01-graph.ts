/**
 * Exercise: Graph (BFS/DFS)
 *
 * Goal: An undirected graph (adjacency list): addVertex, addEdge, bfs(start) and dfs(start) returning the vertices reachable from start (start first).
 *
 * Prerequisites:
 *   - adjacency list representation
 *   - BFS with a queue
 *   - DFS with recursion/stack
 *   - visited set
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Graph_(abstract_data_type)
 *
 * Time budget: ~35 minutes.
 */

export class Graph {
  addVertex(_v: string): void {
    throw new Error('not implemented');
  }
  addEdge(_a: string, _b: string): void {
    throw new Error('not implemented');
  }
  bfs(_start: string): string[] {
    throw new Error('not implemented');
  }
  dfs(_start: string): string[] {
    throw new Error('not implemented');
  }
}
