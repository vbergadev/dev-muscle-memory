/**
 * Exercise: decimalToBinary
 *
 * Goal: Convert a non-negative integer to its binary string representation using a stack (push remainders, pop to build the string).
 *
 * Prerequisites:
 *   - the stack from exercise 01
 *   - repeated division by 2
 *   - reading remainders bottom-up
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Binary_number
 *
 * Time budget: ~15 minutes.
 */

export function decimalToBinary(n: number): string {
  throw new Error('not implemented');
}
