/**
 * Exercise: Stack (LIFO)
 *
 * Goal: Implement a generic LIFO stack: push, pop (returns and removes the top), peek (reads the top), isEmpty, size.
 *
 * Prerequisites:
 *   - LIFO semantics
 *   - backing the stack with an array
 *   - generics
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Stack_(abstract_data_type)
 *
 * Time budget: ~20 minutes.
 */

export class Stack<T> {
  push(_item: T): void {
    throw new Error('not implemented');
  }
  pop(): T | undefined {
    throw new Error('not implemented');
  }
  peek(): T | undefined {
    throw new Error('not implemented');
  }
  isEmpty(): boolean {
    throw new Error('not implemented');
  }
  size(): number {
    throw new Error('not implemented');
  }
}
