/**
 * Exercise: create a 2D matrix
 *
 * Goal: Return a matrix (array of arrays) with `rows` rows and `cols` columns,
 *   every cell initialized to `fill`. Each row must be an independent array
 *   (mutating one row must not affect the others).
 *
 * Prerequisites:
 *   - nested arrays (number[][])
 *   - the reference-sharing pitfall of Array(n).fill(sameArray)
 *   - nested loops
 *
 * References:
 *   - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Indexed_collections
 *
 * Rules:
 *   - Rows must not share the same array reference
 *
 * Time budget: ~10 minutes.
 */

export function createMatrix(rows: number, cols: number, fill: number): number[][] {
  throw new Error('not implemented');
}
