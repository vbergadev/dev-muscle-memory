/**
 * Exercise: multiply two matrices
 *
 * Goal: Given matrices A (m x n) and B (n x p), return their product (m x p).
 *   Assume the dimensions are compatible (A's columns === B's rows).
 *
 * Prerequisites:
 *   - matrix multiplication definition (dot product of row x column)
 *   - triple nested loops
 *   - indexing number[][]
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Matrix_multiplication
 *
 * Time budget: ~20 minutes.
 */

export function multiplyMatrices(a: number[][], b: number[][]): number[][] {
  throw new Error('not implemented');
}
