/**
 * Exercise: minCoinChange (DP)
 *
 * Goal: Return the minimum number of coins needed to make `amount` from the given coin denominations (unlimited supply). Return -1 if it cannot be made. Use dynamic programming.
 *
 * Prerequisites:
 *   - dynamic programming (bottom-up table)
 *   - why greedy fails for some coin sets
 *
 * References:
 *   - https://en.wikipedia.org/wiki/Change-making_problem
 *
 * Time budget: ~30 minutes.
 */

export function minCoinChange(coins: number[], amount: number): number {
  throw new Error('not implemented');
}
